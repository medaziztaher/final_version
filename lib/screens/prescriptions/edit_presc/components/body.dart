/*import 'package:flutter/material.dart';
import 'package:medilink_app/models/prescription.dart';
import 'package:medilink_app/screens/prescriptions/edit_presc/components/form.dart';



class Body extends StatelessWidget {
  const Body({super.key, required this.prescription, required this.userId});
  final Prescription prescription;
  final String userId;

  @override
  Widget build(BuildContext context) {
    return SafeArea(
      child: SizedBox(
        width: double.infinity,
        child: Padding(
          padding:
              EdgeInsets.symmetric(horizontal: getProportionateScreenWidth(20)),
          child: SingleChildScrollView(
            child: Column(
              children: [
                SizedBox(height: SizeConfig.screenHeight * 0.05),
                EditPrescForm(prescription: prescription, userId: userId),
                SizedBox(height: SizeConfig.screenHeight * 0.08),
              ],
            ),
          ),
        ),
      ),
    );
  }
}*/
